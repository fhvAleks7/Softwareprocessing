name: CI Develop

on:
  push:
    branches:
      - 'develop/**'

jobs:
  pipeline:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run basic command
        run: echo "Pipeline is running successfully on develop!"

      - name: Show Docker version
        run: docker --version

      - name: Generate PDF documentation
        run: docker run --rm -u "$(id -u):$(id -g)" -v "$PWD":/data pandoc/latex documentation.md -o documentation.pdf --pdf-engine=xelatex
        working-directory: docs

      - name: Check output
        run: ls docs/